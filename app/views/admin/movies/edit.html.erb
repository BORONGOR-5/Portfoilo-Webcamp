<div class="body">
  <div class="container">
    <h3>作品登録</h3>
      <div class="row">
        <div class="col-xs-12">
          <%= form_with model: @movie, url: admin_movie_path, local:true do |f| %>
            <div class="col-xs-4"></div>
            <div class="col-xs-8">
              <%= render "form", f: f, genre: @genre %>
              <div>
                <%= f.submit "内容を更新", class: "btn btn-primary btn-lg pull-right" %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
  </div>
</div>
<div class="body">
  <div class="container">
    <h2 class="text-center">作品登録</h2>
    <br><br>
      <div class="row">
        <div class="col-xs-12">
          <%= form_with model: @movie, url: admin_movies_path, local:true do |f| %>
            <div class="col-xs-4"></div>
            <div class="col-xs-8">
              <%= render "form", f: f, genre: @genre %>
              <div>
                <%= f.submit "新規登録", class: "btn btn-primary btn-md center-block" %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
  </div>
</div>
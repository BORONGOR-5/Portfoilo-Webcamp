<div class="text-center" style="margin: 180px 0 50px 0">
  <!--画面遷移前のリンク-->
  <%= link_to "トップ", root_path, data: {"turbolinks" => false} %>
  >
  <%= link_to "ユーザー一覧", members_path, data: {"turbolinks" => false} %>
  >
  <%= link_to member_path(@member), data: {"turbolinks" => false} do %>
    <%= @member.name %>さんの詳細
  <% end %>
  >
  <%= link_to edit_member_path(@member), data: {"turbolinks" => false} do %>
    <%= @member.name %>さんの編集
  <% end %>
  >
  <%= @member.name %>さんの退会確認
  <h2>本当に退会しますか？</h2>
  <p style="margin: 50px 0 50px 0">
        退会すると、ユーザー登録情報の閲覧や、レビューの編集ができなくなります。<br/>
        本当に退会する場合は、「退会する」をクリックしてください。</p>
      <div class="col-lg-12 center-block">
            <%= link_to '退会しない', member_path, class: 'btn btn-primary' %>
        <% if member_signed_in? && @member.id == current_member.id %>
            <%= link_to '退会する', leave_path, method: :update, "data-confirm" => "本当に退会しますか？", class: 'btn btn-danger' %>
        <% end %>
      </div>
  </div>
</div>
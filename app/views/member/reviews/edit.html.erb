<div class="body">
  <div class="container-fluid">
    <br>
    <div class="row">
      <%= render "layouts/errors", obj: @review %>
      <%= render 'layouts/sideber', genres: @genres %>
      <div class="col-xs-offset-1 col-xs-5 row" style="padding-top: 20px">
        <!--画面遷移前のリンク-->
        <%= link_to "トップ", root_path, data: {"turbolinks" => false} %>
        >
        <%= link_to "作品一覧", movies_path, data: {"turbolinks" => false} %>
        >
        <%= link_to movie_path(@movie), data: {"turbolinks" => false} do %>
          <%= @movie.title %>の詳細
        <% end %>
        >
        <%= link_to review_path(@review), data: {"turbolinks" => false} do %>
          <%= @movie.title %>のレビュー詳細
        <% end %>
        >
        <%= @movie.title %>のレビュー編集
        <h2><%= @movie.title %>のレビューを編集する</h2>
        <br>
        <div class="row">
          <div class="col-xs-12">
    	      <%= form_with model: @review, url: review_path, method: :patch, local:true do |f| %>
              <%= render "form", f: f %>
              <div style="margin-right: 110px">
                <%= f.submit "編集内容を保存する",  class: "btn btn-primary btn-md center-block", data: {"turbolinks" => false} %>
              </div>
            <% end %>
    	    </div>
        </div>
      </div>
    </div>
  </div>
</div>
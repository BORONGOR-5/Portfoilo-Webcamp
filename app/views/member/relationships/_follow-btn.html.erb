<% if current_member.following?(member) %>
  <%= link_to "フォロー外す", "/relationships/#{member.id}", method: :delete, class: "btn btn-primary" %>
<% else %>
  <%= link_to "フォローする", "/relationships/#{member.id}", method: :post, class: "btn btn-success" %>
<% end %>
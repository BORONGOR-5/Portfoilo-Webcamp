<% if current_member.following?(@member) %>
<%= link_to "フォロー外す", relationships_path(@member.id), method: :delete, class: "btn btn-primary" %>
<% else %>
  <%= link_to "フォローする", relationships_path(@member.id), method: :post, class: "btn btn-success" %>
<% end %>